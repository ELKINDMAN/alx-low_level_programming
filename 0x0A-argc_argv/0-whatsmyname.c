#include <stdio.h>
#include "main.h"

/**
 * main - entry point of the program.
 * @argc: keeps count of strings in argv.
 * @argv: array of strings
 *
 * Return: Always 0 (success)
 */
int main(int argc _attribute__((unused)), char *argv[])
{
	printf("%s\n", *argv)

	return (0);
}
