#include "main.h"

/**
 * mul - function that multiplies two integers.
 * @a: first integer
 * @b: second integer
 *
 * Return: always 0 (Success)
 */
int mul(int a, int b)
{
	int iden;

	iden = a * b;
	return (iden);
}
